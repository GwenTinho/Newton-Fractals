//concept :

/*
    Make it a function that returns a class based on given params e.g. maxlength, stuff like that
    then use floating pointarithmetic with more than 8 bytes, make it 16 or 32 even
*/

function bigFloatFactory(presets) {
    return class {
        //this. ... = presets. ...
    }
}